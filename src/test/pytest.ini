[pytest]
addopts = -p no:warnings
testpaths =
    spark-test
filterwarnings =
    ignore::DeprecationWarning
markers =
    spark: marks tests as a spark application
    technical: mark a test as a technical test